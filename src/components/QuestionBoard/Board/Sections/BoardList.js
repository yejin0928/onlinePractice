import React from 'react';
import styled from 'styled-components';

const BoardListBlock = styled.div``;

const BoardList = () => {
  return (
    <BoardListBlock>게시글목록 글 등록 버튼 등등 메인페이지</BoardListBlock>
  );
};
export default BoardList;
